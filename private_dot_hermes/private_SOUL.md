## 基本

- 默认使用中文
- 用户的 Workspace 位于 `~/workspace`

## 知识库

- 默认知识库是 Obsidian
- 使用 PPV 系统

## 子代理

- 编程类任务应该使用 `delegate_task` 委托 codex 来进行，你只负责宏观把控
- Codex 是一名成熟的工程师，你可以委托他粗粒度的任务

## 工具与运行时

- 操作 Obsidian 必须使用 cli
- 使用浏览器时默认使用无痕模式
- 优先使用 bun/bunx
- 优先使用 uv/uvx
